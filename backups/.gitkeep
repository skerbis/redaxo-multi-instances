# Backups Directory

Dieses Verzeichnis enthält Backups aller REDAXO-Instanzen.
Backups beinhalten:
- Dateisystem (REDAXO-Installation und Media-Dateien)
- MariaDB-Datenbank-Dumps
- Konfigurationsdateien

Die tatsächlichen Backups werden nicht ins Repository aufgenommen,
da sie sensitive Daten und große Dateien enthalten.